---
- hosts: localhost
  remote_user: root
  connection: local
  vars:
   - singularity_install_package: False

  pre_tasks:
   - name: setup dir
     file:
      path: "{{singularity_conf_dir }}"
      state: directory
  
  roles:
    - ansible-role-singularity
